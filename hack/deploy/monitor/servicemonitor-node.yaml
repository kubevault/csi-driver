apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: csi-vault-node-servicemonitor
  namespace: ${PROMETHEUS_NAMESPACE}
  labels:
    ${SERVICE_MONITOR_LABEL_KEY}: ${SERVICE_MONITOR_LABEL_VALUE}
    component: node
spec:
  namespaceSelector:
    matchNames:
    - ${CSI_VAULT_NAMESPACE}
  selector:
    matchLabels:
      app: csi-vault
      component: node
  endpoints:
  - port: api
    bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
    scheme: https
    tlsConfig:
      caFile: /etc/prometheus/secrets/csi-vault-apiserver-cert/tls.crt
      serverName: "csi-vault-node.${CSI_VAULT_NAMESPACE}.svc"
