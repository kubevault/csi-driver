kind: Pod
apiVersion: v1
metadata:
  name: my-csi-app
spec:
  containers:
    - name: my-frontend
      image: busybox
      volumeMounts:
      - mountPath: "/data"
        name: my-vault-volume
      command: [ "sleep", "1000000" ]
  volumes:
    - name: my-vault-volume
      persistentVolumeClaim:
        claimName: csi-pvc
        readOnly: true
      secret:
        secretName: "xy"




